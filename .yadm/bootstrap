#!/usr/bin/env sh

system_type=$(uname -s)

if [ "${system_type}" = "Darwin" ]; then
  setup/install-homebrew.sh
  setup/install-vim.sh
  setup/install-zsh.sh
  setup/install-fonts.sh
  setup/install-iterm2.sh
fi
