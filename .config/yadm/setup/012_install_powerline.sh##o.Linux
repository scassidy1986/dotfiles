#!/bin/bash

source .env
source ${HOME}/.logging
source .helpers-linux
source .helpers

log_info "$0 $@"

log_info "=> Installing Powerline fonts..."

install_dependency "fonts-powerline"

log_info "Done!"
